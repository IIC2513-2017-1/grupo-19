<%= form_for(raffle_category) do |f| %>
  <% if raffle_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(raffle_category.errors.count, "error") %> prohibited this raffle_category from being saved:</h2>

      <ul>
      <% raffle_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <li><%= f.label :name %></li>
    <li><%= f.text_field :name %></li>
  </div>

  <div class="field">
    <li><%= f.label :description %></li>
    <li><%= f.text_area :description %></li>
  </div>

  <div class="actions">
    <%= f.submit class:"button"%>
  </div>
<% end %>

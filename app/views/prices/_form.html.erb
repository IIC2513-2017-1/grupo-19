<%= form_for(price) do |f| %>
  <% if price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(price.errors.count, "error") %> prohibited this price from being saved:</h2>

      <ul>
      <% price.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :raffle_id %>
    <%= f.number_field :raffle_id %>
  </div>

  <div class="field">
    <%= f.label :price_category_id %>
    <%= f.number_field :price_category_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

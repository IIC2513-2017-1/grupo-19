<%= form_for(raffle) do |f| %>
  <% if raffle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(raffle.errors.count, "error") %> prohibited this raffle from being saved:</h2>

      <ul>
      <% raffle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.number_field :price %>
  </div>

  <div class="field">
    <%= f.label :final_date %>
    <%= f.datetime_select :final_date %>
  </div>

  <div class="field">
    <%= f.label :collected_money %>
    <%= f.number_field :collected_money %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :raffle_category_id %>
    <%= f.number_field :raffle_category_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
